class SearchProcessor {
    // try without jquery?
    constructor(
        // init webstorage & loop over all rows/data if there and add to storage
    )

    registerHandlers(doc) {

    }
}

$(document).ready(function () {
    new SearchProcessor().registerHandlers(document);
});
